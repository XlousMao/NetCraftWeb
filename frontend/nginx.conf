server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 由外层 nginx 统一代理；这里兜底转发
    location /api/ {
        proxy_pass http://backend:8000;
    }
}
