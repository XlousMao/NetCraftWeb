# 2026-08-21 工作日志

## GEAP（游戏经济分析平台）全栈开发

按规格书完成 Phase 0→8 全量自动开发，交付可用平台。

**成果**：
- 后端 FastAPI + SQLAlchemy 2.x + Pydantic 2.x 分层架构（models/schemas/repositories/services/api/analysis）
- 前端 Vue3 + TS + Element Plus + Pinia + ECharts + vis-network
- Docker Compose + Nginx + Alembic 迁移 + 14 个 pytest + Demo 数据
- Git 已初始化，3 次提交（feat / fix / chore）

**关键决策**：
- 本地默认 SQLite（零依赖启动），生产切 PostgreSQL（`DATABASE_URL` 一行配置）
- Postgres 驱动用 `psycopg[binary]`（`psycopg2-binary` 在 Py3.13 无 wheel）
- 估值 auto 策略：manual → vendor → market（对齐规格"掉落价值=数量×商人收购价"）
- 新增 `dungeon_repairs` 表保存维修明细快照
- AI 无 key 时降级为本地规则化分析
- 后端 venv 位于 `C:\Users\25805\.workbuddy\binaries\python\envs\geap`

**验证状态**：
- 后端 14 测试通过；API 冒烟测试通过；migration→seed 全流程通过
- 前端 `npm run build` 通过（ECharts/Element Plus 大包提示属正常）
- 服务运行：后端 8000、前端 5173

**待办/后续**：OCR/截图识别、自动价格采集、AI 预测等扩展项未实现（规格列为未来路线图）。

## Docker 排障（用户 Docker Desktop 环境）

- 现象：`docker compose up -d --build` 拉取 `python:3.12-slim`/`node:22-alpine` 失败，报 `dial tcp [2a03:2880:...]:443 connectex`（IPv6 无效地址 = Docker Hub DNS 被污染）。
- 根因：国内网络访问 Docker Hub 受限，未配置镜像加速。
- 关键教训：**Docker Desktop（Windows）会覆盖 `~/.docker/daemon.json`，且启用 containerd snapshotter 后 daemon 镜像配置不生效**，守护进程跑在 WSL2 VM 里，镜像只能通过 GUI（Settings→Docker Engine）配置。
- 最终方案（已验证跑通）：
  1. 直接从可用镜像源 `docker.1ms.run` 预拉取基础镜像并 `docker tag` 成标准名（python:3.12-slim / node:22-alpine）。
  2. Dockerfile 增加 `PIP_INDEX_URL`/`NPM_REGISTRY` 构建参数（默认官方源），compose 从 `.env` 透传，国内用清华 pypi + npmmirror。
  3. Nginx 加 `resolver 127.0.0.11` + 变量 `proxy_pass` 动态解析上游，修复后端容器重建后 IP 变化导致 502。
- 用户环境：Docker Desktop 29.6.1（Windows），守护进程运行中。
- 最终部署：5 容器全 Up（geap-db/geap-redis healthy），http://localhost/ 全链路 200，本周净利润 3068。

## V2 奶块经济领域重构（NetCraftWeb）

按 V2 规格在现有代码上扩展（未推翻重做），完成 P0+P1+P2 大部分。

**核心成果**：
- 货币体系：CurrencySystem/Denomination/ConversionRule（图遍历换算，1钻石结晶=99钻石，不硬编码）
- RMB：FiatExchangeObservation + FiatService（99钻石块=27.1RMB → 1钻石≈0.0304RMB，按时间点生效）
- Decimal 化：经济字段 Float→Numeric(20,8)，economy_calculator 用 Decimal；时间字段除外
- ItemRole 多角色；维修去掉 currency_cost 改纯 item+quantity
- 副本/维修估值快照补 base_currency_value + fiat_value
- 26 个测试通过（test_currency/rmb/historical_price/repair/dungeon...）
- 前端：货币体系配置页 + Dashboard/副本/物品详情显示钻石+RMB

**关键坑（复用注意）**：
- SQLAlchemy 多外键指向同一表时，relationship 需显式 foreign_keys
- 货币物品（钻石块等）本身无价格，估值要走 CurrencyService.to_base_factor 快路径
- record_price 历史价不应覆盖当前价（当前价=最新 observed_at 的价格）
- Alembic Float→Numeric 在 PG 需 postgresql_using；新增 NOT NULL 列需 server_default
- Pydantic response_model 会丢弃未声明的字段（current_value 需显式加进 schema）
- SQLAlchemy 列名 `metadata` 与 ORM 保留属性冲突，用 `metadata_` + validation_alias

**升级说明**：V1→V2 货币单位从 gold 变钻石，旧数据无法无损迁移，需 `docker compose down -v` 重建（README 已写明）。

## 副本管理 + 停用副本过滤（用户改动 + 修复）

- 用户新增：副本管理页编辑/停用开关/软删除；主图上传同步 icon_url；快速录入物品带图片。
- 修复：`/api/dungeons` 默认只返回启用副本（新增 `include_inactive` 参数），管理页用 `list(true)` 显示全部，副本记录下拉默认只有启用副本。
- 教训：列表接口的「软删除过滤」应在后端做（默认过滤 + 显式参数），前端过滤只作兜底。

## 物品库编辑/删除 + 副本记录编辑 + 掉落物图片

- 物品卡片悬停显示编辑/删除按钮；编辑弹窗支持角色多选；软删除。
- 后端新增 `PUT /dungeon-runs/{id}` 编辑副本记录；`DungeonService` 抽出 `_apply_details` 复用创建/更新的估值逻辑（更新 = 删旧明细 + 重算）。
- 掉落/消耗/维修返回 `icon_url` + repairs 明细，副本记录列表用缩略图展示掉落物。
- 编辑副本记录时副本下拉显示全部（含停用，否则无法选中已停用副本的记录），新建时仅启用副本。

## 装备/配方/生产/活动编辑 + 材料图片 + 活动一致性

- 装备编辑（PATCH）支持更新维修需求；配方编辑（PUT）支持更新材料/产出并同步关系；生产记录/活动记录支持 PUT 编辑 + DELETE 删除。
- 材料/产出（RepairRequirement/RecipeMaterial/RecipeOutput）返回 `icon_url`，新增 `ItemChip` 组件用「图片 + 名称 + 数量」展示。
- 数据一致性：删除副本记录/生产记录时 `ActivityService.delete_by_ref` 级联删活动记录；`cleanup_orphans` 在启动时清理引用已不存在记录的孤儿活动记录。
- 生产记录计算逻辑抽出 `_compute_fields` 复用（创建/更新）。

## 副本记录掉落物图片选择 + 数量四则运算

- 掉落/消耗品 `el-select` 选项内嵌图片（`el-option` 自定义内容），添加后以 `ItemChip`（图片+名称+数量+可关闭）展示。
- 数量输入用 `el-input` + `calcExpr`（正则清理 + `new Function` 安全计算），支持 `3+2`、`5*3`、括号等，失焦/回车自动计算。
- 列表接口「软删除过滤」统一模式：默认过滤 `is_active`，加 `include_inactive` 参数（副本/装备/配方一致）。

## V3 奶块经济分析平台（领域模型升级，用户先确认方案再编码）

用户给出 10 阶段规格，要求先分析→输出方案→确认后编码。确认的 3 个决策：
1. **副本利润彻底动态化**（DungeonLoot/Cost/Repair 只存 item_id+quantity，删快照，利润按时间动态查历史价）
2. **价格默认映射**（vendor→NPC_PRICE、market→SELL_OFFER、manual→MANUAL_ESTIMATE，迁移后删旧字段）
3. **保留现有命名**（factor/currency_item_id 等不重命名）

**核心变更**：
- `market_observations` 表（observation_type + quantity + price_item_id + price_quantity 以物易物），价格是市场事件而非 Item 属性；删除 items 三价格字段 + item_price_history。
- `ValuationService.get_unit_price` 从市场观察按 `observed_at<=target` 查历史价，auto 优先级 MANUAL>NPC>SELL_OFFER。
- `compute_run_economy(db, run)` 动态计算单次副本经济（钻石+RMB），供 analyze_period/dungeon_rankings/activity/_run_out 复用。
- `analysis/decision.py`：analyze_craft_vs_buy / analyze_recipe_decision / analyze_dungeon_decision。
- Recipe 加 `recipe_type`（ALCHEMY/CRAFT/SYNTHESIS）。
- 迁移 `a1b2c3d4e5f6` 手动编写（含价格数据迁移 INSERT 到 market_observations）。

**关键坑**：
- SQLite 跑不了 V2 的 `ALTER COLUMN TYPE`，迁移只适配 PostgreSQL（`postgresql_using`），本地开发用 create_all。
- 测试里业务时间（started_at）不能早于价格观察时间，否则查不到历史价返回 0。
- `total_duration_minutes` 从落库字段改为 `@property`（travel+combat 派生）。

## 市场观察录入优化（三面额计价）

- 需求：收购/出售挂单最常用需突出；数量给预设（99/33）；价格不能只填钻石，要支持钻石/钻石块/钻石结晶三面额输入并实时换算总价。
- 实现：纯前端（`ItemDetail.vue`）。观察类型改 `el-radio-button`；数量加 `PRICE_PRESETS=[1,33,99]` 快捷 tag；价格用 `price_parts`（面额 item_id→数量，默认全 0）+ `totalDiamond` computed（面额×base_value 求和）。
- 面额与换算系数来自 `currencyApi.summary()`（`/currency/summary` 返回 denominations 的 base_value），不硬编码 9/99；无货币体系时降级为单一「钻石」面额。
- 提交时 `price_quantity=总钻石数`、`price_item_id=基础货币item_id`，后端模型零改动。
- 注意：git 历史里用户自提交了 `e38624d 完成升级，并且修复移植问题`（在 V3 之后），改动前需先看 git log 确认基线。

## 市场观察记录编辑/删除 + 图表自适应 + RMB 换算

- 后端新增 `PATCH/DELETE /items/{id}/market/{obs_id}`（`MarketObservationUpdate` schema，exclude_unset 部分更新）。
- 前端表格加编辑/删除操作列；编辑时用 `splitToDenominations`（贪心从大到小分解）把总钻石回填三面额输入，支持保存/取消。
- **图表自适应坑**：ECharts 在 `el-tab-pane` 隐藏容器里 init 宽度为 0，切到该 tab 后不触发 window resize → 图表被压扁很小。修复：Chart.vue 用 `ResizeObserver` 监听容器尺寸（tab 切换/窗口缩放/侧栏折叠都能触发），替代 window resize。
- 录入价格实时换算 RMB：`totalRmb = totalDiamond × currency.rmb_rate`（rmb_rate 来自 /currency/summary）。

## 副本掉落价值兜底 + 市场观察RMB快照 + 配方/掉落物体验

- **关键坑：估值 fallback 与「未来价格不可用」的平衡**。用户「先录副本(started_at 过去)、后补价格(observed_at 现在)」时，`compute_run_economy` 用 started_at 查历史价查不到 → gross_value=0。解决：`get_unit_price` 按时间查不到时兜底用 `_latest_observation`，但**兜底也必须加 `observed_at <= now` 上限**（否则破坏 test_no_future_price「未来价格不可用」）。即：优先当时价 → 无则用「截至现在」最新价 → 仍无才 0。
- 市场观察记录返回 `fiat_value`（按 observed_at 汇率算的总 RMB，`observation_fiat` 方法），前端表格加「RMB(当时)」列。
- 配方录入用图片下拉（el-option 内嵌 img）+ ItemChip 展示；SYNTHESIS/CRAFT 成功率自动 100%（确定性操作），仅 ALCHEMY 显示成功率滑块。
- 副本掉落物去掉 `.slice(0,5)` 折叠，全部 flex-wrap 展示。
- 物品详情 header 加「自制 vs 购买」对比块（buy_price vs minCraftCost + 推荐 tag），点击跳 decision tab。

## 配方删除后无法重建同名（软删除释放唯一名）

- **坑**：`Recipe.name` 有 DB 唯一约束，软删除只置 `is_active=false`，名字仍占用 → 同名配方无法重建，且「显示已停用」里重复点删除只是 no-op。
- 解决：
  1. `delete_recipe` 对启用配方软删除时改名 `{name}__deleted_{id}` 释放唯一名；
  2. `create_recipe` 查重加 `is_active.is_(True)`（停用同名不再拦截）；
  3. 对已停用配方再次删除走**物理删除**（清理 ProductionRecord、活动引用、`RelationService.clear_recipe_relations` 清物品关系）。
- 前端删除按钮按 `row.is_active` 区分「删除」/「彻底删除」。
- 通用结论：**凡带 DB 唯一约束的实体做软删除，都必须同时释放唯一键（改名/置空）**，否则无法重建同名。

## 制作采购方案（合成表 + 材料需求 + 采购地点优化）

- 新增 `analysis/decision.py::analyze_crafting_plan(db, item_id, target_quantity)` + `GET /analysis/crafting-plan`。
- 核心逻辑：
  1. 配方逆向拆解：找所有产出该物品的启用配方（`RecipeOutput.item_id==目标` join Recipe is_active）。
  2. 合成次数 `int((target/effective).to_integral_value(ROUND_CEILING))`，effective=产出数量×成功率（炼金失败多备料）；材料总需求=单次材料×次数。
  3. 采购价：只取 `SELL_OFFER` 按 `location` 分组取最低单价，推荐最便宜地点，带 observed_at 时间戳。
  4. 自制总成本 vs 直接购买（buy_total=买价×target），推荐 craft/buy + 节省金额。
- 物品详情新增「合成表」tab（数量 1/33/99 可调）；`ItemOut.craftable`（被配方 PRODUCES）→ 物品库卡片「可合成」角标。
- 市场观察录入加独立「地点」字段（location），表格单独展示地点列（之前 location 后端有但前端从未暴露）。
